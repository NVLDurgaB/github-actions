name: Variable in develop
on: 
  push:
    branches:
      - develop
env:
  MY_VARIABLE: ${{ secrets.MY_VARIABLE }}
jobs:
  print-variable:
    runs-on: ubuntu-latest
    steps:
      - name: Print Variable
        run: echo "The value of the variable is:" $MY_VARIABLE
  deploy:
    runs-on: ubuntu-latest
    needs: print-variable
    if: ${{ secrets.MY_VARIABLE == 'Secret value'}}
    steps:
      - name: Deploy Application
        run: echo "Deploying application... MY_VARIABLE is matched!"  

